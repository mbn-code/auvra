// REMOVED: LiveToast previously displayed fabricated purchase notifications
// (random city + item names) to simulate social proof. This is prohibited under
// EU Directive 2019/2161 (Omnibus Directive) as an unfair commercial practice.
// Component is kept as an empty stub to avoid import errors during migration.
// All call sites in layout.tsx have been cleaned up.

export default function LiveToast() {
  return null;
}
